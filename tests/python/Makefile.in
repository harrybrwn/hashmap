PY=python

py-test: $(PyTestDir)/hashmap.%.so
	$(PY) $(PyTestDir)/test.py

$(PyTestDir)/hashmap.%.so:
	$(PY) ./$(PyTestDir)/setup.py build --build-lib $(PyTestDir)

py-clean:
	@for f in `find $(PyTestDir) -name '*.so'`; do rm $$f; done

.PHONY: py-test